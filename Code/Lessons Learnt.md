Or in other words things that beginners should avoid

- Importing Qiskit is now a chore
	- pip install 'qiskit>=1.0'
		- this is because qiskit has dropped dependencies with qiskit-terra (but somehow not qiskit-aer) 